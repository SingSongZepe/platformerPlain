package com.example.platformerplain.pattern;

/**
 * Decorator Pattern
 * Used to dynamically add responsibilities or behaviors to objects without modifying existing classes
 *
 */
public interface Decorator {
    void doSomething();
}
