package com.example.platformerplain.pattern;

/**
 * Strategy Pattern
 * Used in Controller to allow algorithm or behavior replacement
 * or extension without modifying client code.
 */
public interface Strategy {
    void execute();
}
